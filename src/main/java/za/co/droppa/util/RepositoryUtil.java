package za.co.droppa.util;


public final class RepositoryUtil {
    /*
    private static final MongoDBConnection connection = new MongoDBConnection();

    public static final BookingRepository bookingRepository = new BookingRepository(connection.connectToDatabase());

    public static final ContactRepository contactRepository = new ContactRepository(connection.connectToDatabase());

    public static final PersonRepository personRepository = new PersonRepository(connection.connectToDatabase());

    public static final RetailRepository retailRepository  = new RetailRepository(connection.connectToDatabase());

    public static final WhitelistingRepository whitelistingRepository = new WhitelistingRepository(connection.connectToDatabase());

     */
}
